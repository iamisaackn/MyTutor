<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="?android:attr/selectableItemBackground"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Inner LinearLayout to contain item details -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_margin="8dp"
        android:elevation="5dp"
        android:gravity="center"
        android:padding="16dp"
        android:background="@color/white">

        <!-- TextView to display test number -->
        <TextView
            android:id="@+id/testNo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:textColor="@color/black"
            android:textAlignment="center"
            android:textSize="20sp"
            android:text="Title"/>

        <!-- Horizontal ProgressBar to show test progress -->
        <ProgressBar
            android:id="@+id/testProgressbar"
            style="@android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_marginTop="16dp"
            android:layout_marginStart="80dp"
            android:layout_marginEnd="80dp"
            android:progressTint="@color/colorPrimary"
            android:layout_height="wrap_content"/>

        <!-- TextView to display score -->
        <TextView
            android:id="@+id/scoretest"
            android:layout_width="match_parent"
            android:textColor="@color/colorPrimary"
            android:textAlignment="center"
            android:layout_height="wrap_content"/>

    </LinearLayout>
</LinearLayout>